project: ai_data_warehouse
registry: registry.db
provider: local
offline_store:
  type: file
online_store:
  type: redis
  connection_string: "${REDIS_HOST:-localhost}:${REDIS_PORT:-6379}"
entity_key_serialization_version: 2
